using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class trailAudio : MonoBehaviour
{
    public AudioSource whistle;
    // Start is called before the first frame update
    void Start()
    {
        whistle.Play();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
